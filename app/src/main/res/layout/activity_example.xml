<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        tools:context="net.coding.mart.setting.ExampleActivity">

        <com.astuetz.PagerSlidingTabStrip
            android:id="@+id/tabs"
            android:layout_width="match_parent"
            android:layout_height="44dp"
            app:pstsDividerColor="@color/transparent"
            app:pstsIndicatorColor="@color/font_blue"
            app:pstsIndicatorHeight="2dp"
            app:pstsShouldExpand="true"
            app:pstsTabBackground="@color/white"
            app:pstsTabPaddingLeftRight="0dp"
            app:pstsTabTextAllCaps="false"
            app:pstsTabTextColor="@drawable/example_tab_text_color"
            app:pstsUnderlineColor="@color/divide_line"
            app:pstsUnderlineHeight="0.5dp" />

        <android.support.v4.view.ViewPager
            android:id="@+id/viewPager"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:layout_behavior="@string/appbar_scrolling_view_behavior" />

    </LinearLayout>

    <net.coding.mart.common.widget.EmptyRecyclerView
        android:id="@+id/emptyView"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

</FrameLayout>
